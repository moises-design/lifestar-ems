// Active locale for V2 content. When Spanish is added, this module
// becomes the locale switch point (e.g. read from a LocaleContext or
// route prefix) without touching any component code.
import { en } from './en'

export const content = en
